<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <item>
      <title>The Hidden Cost of ai_parse_document in Production</title>
      <link>https://xebia.com/blog/hidden-cost-ai-parse-document-production-databricks/</link>
      <guid>https://xebia.com/?p=152231</guid>
      <pubDate>Wed, 10 Jun 2026 10:15:48 +0000</pubDate>
      <dc:creator>Andy Ho</dc:creator>
      <content:encoded><![CDATA[<div>
  <h1>The Hidden Cost of ai_parse_document in Production</h1>
  <p>ai_parse_document + ai_query makes PDF extraction look easy. Here&#8217;s an honest evaluation of the Databricks-native pipeline for healthcare documents</p>
</div>

<article>
  <p>A colleague and I were exploring options for a healthcare project that had hit a familiar wall. The structured data was usable — dates, diagnoses, billing records — but the information people actually needed lived elsewhere: discharge summaries, referral letters, intake forms, nursing notes. All unstructured and inconsistent.</p>



<p>We were weighing up OCR tools, custom parsers, third-party APIs — the usual trade-offs of cost, accuracy, and maintenance — when Databricks released a new Function, <a href="https://docs.databricks.com/aws/en/sql/language-manual/functions/ai_parse_document" rel="nofollow noopener" target="_blank"><code>ai_parse_document</code></a>, which seemed to cut straight through all of that.</p>



<p>It was a few lines of SQL and messy PDFs came out the other side as structured JSON. The first time you see it work, it genuinely feels like magic — a proof of concept in an afternoon.</p>



<p>That simplicity, though, is a bit deceptive. Because once you move beyond the demo, the problem changes. It is no longer just about extracting fields from a document. It is about running that extraction as a system: reliably, repeatedly, and at scale.</p>



<p class="has-text-align-left">That shift — from query to system design — is where certains problems start to surface. That is what this post is about.</p>



<p>The examples use <a href="https://www.databricks.com/" rel="nofollow noopener" target="_blank">Databricks</a> – specifically <a href="//docs.databricks.com/aws/en/sql/language-manual/functions/ai_parse_document">ai_parse_document</a> and <a href="https://docs.databricks.com/en/machine-learning/foundation-models/ai-functions.html#ai-query" rel="nofollow noopener" target="_blank"><code>ai_query</code></a> – in SQL or notebooks. You&#8217;ll need <a href="https://docs.databricks.com/aws/en/sql/language-manual/functions/ai_parse_document" rel="nofollow noopener" target="_blank">Databricks Runtime 17.1+</a> and basic familiarity with <a href="https://docs.databricks.com/delta/tables.html" rel="nofollow noopener" target="_blank">Delta tables</a> and the <a href="https://www.databricks.com/glossary/medallion-architecture" rel="nofollow noopener" target="_blank">medallion architecture</a>.</p>



<p></p>



<p><strong>What this post cover:</strong> </p>



<p>This is not a benchmark and not a full production tutorial. It is an engineering evaluation of a Databricks-native document extraction pattern: where it shines, where the hidden costs appear, and when a deterministic alternative is a better fit. The snippets run in a Databricks notebook and are simplified to highlight key patterns; the <a href="https://github.com/AndyMDH/databricks_ai_document_parse" rel="nofollow noopener" target="_blank">companion notebook</a> has the complete runnable implementation. The examples use synthetic patient data.</p>



<p></p>



<p><strong>What It doesn&#8217;t cover: </strong></p>



<p>Fine-grained cost optimisation (Photon vs. serverless, token reduction strategies, model endpoint pricing) is a post of its own. So are production hardening topics like failure modes, exactly-once semantics, and Delta Expectations. Data residency, PII handling at the document level, and Unity Catalog lineage and governance are also out of scope here — each worth a dedicated treatment.</p>



<p></p>



<h2 class="wp-block-heading"><strong>The Starting Point: ai_parse_document + ai_query</strong></h2>



<p>Before looking at the SQL, we need to establish quickly that not all PDFs are the same.</p>



<p><a href="https://veridiansoftware.com/knowledge-base/ingesting-born-digital-pdfs-into-veridian" rel="nofollow noopener" target="_blank">Digitally-born PDFs</a> have real, selectable text baked in – think of saving a Word document as a PDF. Scanned PDFs are photos of pages. The words are visible to a human, but to a computer the page is just an image until <a href="https://www.tungstenautomation.com/learn/blog/ocr-in-pdf-conversion-how-it-works-why-it-matters-2025" rel="nofollow noopener" target="_blank">OCR</a> (Optical Character Recognition) reconstructs the text. This distinction matters because extraction quality differs sharply between them.</p>





<p><strong>Figure 1: Digitally-born PDF (left) vs scanned PDF (right).</strong></p>



<p>In the PoC below, we&#8217;ll demonstrate what <code>ai_parse_document</code> and <code>ai_query</code> looks like in practice:</p>



<pre class="wp-block-code"><code>WITH text_extracted AS (
    SELECT
        substring_index(path, '/', -1) AS filename,
        concat_ws('\n', transform(
            ai_parse_document(content, map('version', '2.0')):document:elements,
            element -&gt; element:content
        )) AS full_text
    FROM read_files('/path/to/pdfs/', format =&gt; 'binaryFile')
)
SELECT
    filename,
    ai_query(
        'databricks-claude-sonnet-4',
        concat('Extract as JSON: {"patient_ref": "...", ...}', full_text)
    ) AS extracted
FROM text_extracted
LIMIT 5;</code></pre>



<p>Let&#8217;s walk through what&#8217;s happening in 2 steps:</p>



<p><strong>Step 1 — Parse the document.</strong> The <code>text_extracted</code> CTE calls <code>ai_parse_document</code> on each binary file loaded by <code>read_files</code>. <code>ai_parse_document</code> handles both document types: for digitally-born PDFs it reads the embedded text directly and for scans it runs OCR to reconstruct it first.</p>



<p>The result is a structured <code>document.elements</code> array — text blocks, tables, section headers. Since you usually just want the text, <code>transform</code> and <code>concat_ws</code> flatten everything into a single string, <code>full_text</code>.</p>



<p><strong>Step 2 — Extract the fields.</strong> The outer <code>SELECT</code> passes that assembled text to <code>ai_query</code> with a prompt: &#8220;extract these five fields as JSON.&#8221; The fields — <code>patient_ref</code>, <code>document_date</code>, <code>document_type</code>, <code>primary_diagnosis</code>, <code>follow_up_required</code> — are enough to build a queryable patient cohort and flag who needs a follow-up. It gets the structured fields right most of the time. When something is ambiguous or unstated, it infers — and does not tell you it did unless you look closely.</p>



<p>Here is a single row from the query result, before any post-processing:</p>



<pre class="wp-block-code"><code>Filename: `admission_PT_2024_00502_digital.pdf`

Here is the extracted data in JSON format:

```json
{
  "patient_ref": "PT-2024-00502",
  "document_date": "2024-03-28",
  "document_type": "admission_note",
  "primary_diagnosis": "Subarachnoid haemorrhage",
  "follow_up_required": true
}
```

&gt; Note: The `follow_up_required` field is set to `true` based on the fact that the patient is being admitted to the hospital and a management plan is being put in place, which typically involves follow-up care. However, this field is not explicitly stated in the document, so this is an inference based on the context.</code></pre>



<p>A few things to notice here. Because&nbsp;we’re&nbsp;using&nbsp;ai_query, the&nbsp;extracted&nbsp;column is not a JSON object&nbsp;&#8211;&nbsp;it is a plain text string. The model leads with a prose sentence, wraps the JSON in markdown code fences, and appends a “Note:” explaining its reasoning. All of that arrives as one continuous string and needs cleanup before the JSON inside can be parsed.&nbsp;</p>



<p>It’s&nbsp;also important to note that this is inference, not extraction. The model made a judgement call on&nbsp;follow_up_required&nbsp;and&nbsp;volunteered&nbsp;an explanation&nbsp;&#8211;&nbsp;but it will not always do that, and it can be confidently wrong either way.&nbsp;</p>



<h2 class="wp-block-heading">Where the Cracks Show&nbsp;</h2>



<p>At this point in time, the query works, however, the first run is not the problem,&nbsp;the second is&nbsp;&#8211;&nbsp;and it surfaces four issues that&nbsp;don’t&nbsp;exist in a notebook:&nbsp;</p>



<p><strong>Cost:</strong>&nbsp;Both functions charge on every run. Without a checkpoint, every run processes every document – and every prompt change re-runs everything. The problem is not the first bill. It is that every correction, prompt revision, and reprocessing cycle reopens the meter.&nbsp;</p>



<p>In healthcare, documents arrive in irregular batches – end-of-month exports, legacy migrations, corrected records re-sent in bulk. At&nbsp;<a href="https://www.databricks.com/product/pricing/ai-parse" target="_blank" rel="noreferrer noopener nofollow">medium-complexity rates</a>&nbsp;(~$4.20–$4.55 per 1,000 pages, using the pricing available at the time of writing), a single corpus of 30,000 pages costs $120&nbsp;– $137 to parse. That is manageable once. But prompt iteration is not a one-time cost – every prompt refinement re-runsai_query&nbsp;across every&nbsp;document, and&nbsp;onboarding a new&nbsp;hospital’s&nbsp;format can trigger a full re-parse.&nbsp;</p>



<p><strong>Duplicates:</strong>&nbsp;Hospitals send the same document more than once – a corrected discharge summary when the original had the wrong diagnosis code, a re-sent referral letter after a system migration. Nothing in the query above catches that. Both versions land in Silver: the same patient appears twice in your cohort query, and if the correction changed the diagnosis, both versions of it appear too.&nbsp;</p>



<p><strong>Non-determinism:</strong>&nbsp;This is an uncomfortable part: your pipeline can be “correct” and still produce different answers on different days.&nbsp;</p>



<p>A traditional rule-based parser always produces the same output for the same input. An LLM&nbsp;doesn’t.&nbsp;ai_query&nbsp;<a href="https://docs.databricks.com/aws/en/sql/language-manual/functions/ai_query" target="_blank" rel="noreferrer noopener nofollow">defaults to temperature 0</a>, which makes it more predictable – but&nbsp;<a href="https://arxiv.org/html/2408.04667v5" target="_blank" rel="noreferrer noopener nofollow">even temperature 0 doesn’t guarantee identical outputs</a>. Floating-point rounding and GPU parallelism introduce subtle variation regardless.&nbsp;</p>



<p>In practice,&nbsp;follow_up_required&nbsp;can flip between&nbsp;true&nbsp;and&nbsp;false&nbsp;for the same document on different runs&nbsp;&#8211;&nbsp;no error, no warning, just quietly different results. In a regulated environment, that breaks auditability: your reporting changes without any change in the underlying data.&nbsp;</p>



<p>There are ways to mitigate this by running extraction multiple times and taking the most common result, or building a labelled test set to evaluate your prompt against known-good outputs. That second approach is especially worth the investment when you update the prompt to support a new document type or hospital format: without a test set, you have no way of knowing whether the change improved extraction for the new format without quietly breaking something for an existing one. Just note that running multiple passes per document multiplies your&nbsp;ai_query&nbsp;costs directly&nbsp;&#8211;&nbsp;non-determinism and cost are not separate problems here.&nbsp;</p>



<p><strong>Input noise.</strong> Every page has “Confidential &#8211; Amsterdam UMC &#8211; Page 4 of 11”, for example, stamped on it, and that goes to the LLM too – consistent per hospital but meaningless for extraction. Because ai_parse_document handles the binary internally, noise arrives pre-baked into the text; you handle it in Silver rather than before the LLM sees it. Headers bleeding into free-text fields are the most common symptom – a primary_diagnosis that starts with the hospital letterhead instead of the actual diagnosis. </p>



<p>None of these are fatal. But all of them require system design the demo hides. </p>



<h2 class="wp-block-heading">Bronze: Where the LLM Stops&nbsp;</h2>



<p>Once you stop treating the query as the product and start treating it as ingestion, streaming with checkpointing becomes the natural design. </p>



<p>One thing worth flagging before going further. The actual PII concern here is the full document text: discharge summaries and clinical notes contain names, dates of birth, and diagnoses, and all of that content is sent to ai_query. Before running this on real patient data, you need to either strip identifying information from the text before it reaches the LLM, or confirm that your data processing agreements cover routing clinical content. As we mentioned at the beginning, this sits outside the scope of this discussion but it is worth being acutely aware of before you build. </p>





<p><strong><em>Figure 2: From PDFs to&nbsp;queryable&nbsp;clinical data.&nbsp;</em></strong><strong><em>cloudFiles</em></strong><strong><em>&nbsp;streams new files automatically;&nbsp;</em></strong><strong><em>ai_parse_document</em></strong><strong><em>&nbsp;parses each binary into a structured elements array; the assembled text goes to&nbsp;</em></strong><strong><em>ai_query</em></strong><strong><em>&nbsp;for field extraction; Bronze stores the result with a streaming checkpoint; Silver unnests the struct into a flat,&nbsp;queryable&nbsp;table.</em></strong>&nbsp;</p>



<p>Our first instinct at this point is to stay in SQL: hash each file, track&nbsp;what’s&nbsp;been processed in a Delta table, and version by prompt. That works, but&nbsp;it&#8217;s&nbsp;a boilerplate&nbsp;you write and&nbsp;maintain&nbsp;yourself.&nbsp;</p>



<p><strong>Structured Streaming</strong>&nbsp;is a cleaner path. Spark manages state automatically via checkpoints&nbsp;&#8211;&nbsp;new files flow through, already-processed ones are skipped, and nothing re-runs the LLM unnecessarily. Less boilerplate, less to get wrong.&nbsp;</p>



<p><strong>One practical note:</strong>&nbsp;patient_ref&nbsp;is one hospital’s local ID. The same patient will have a completely different ID at the next institution. Store the source hospital identifier alongside every&nbsp;patient_ref&nbsp;from the start. One line now, months of pain if you skip it.&nbsp;</p>



<p>The pipeline splits into three streaming tasks, each writing to its own Delta table and&nbsp;maintaining&nbsp;its own checkpoint:&nbsp;</p>



<pre class="wp-block-code"><code># Task 1: Parse each PDF once – checkpoint prevents re-processing
(
    spark.readStream.format("cloudFiles")
    .load("/path/to/pdfs/")
    .select("path", ai_parse_document("content").alias("parsed"))
    .writeStream.toTable("bronze_parsed_raw")
)

# Task 2: Assemble elements into full text
(
    spark.readStream.table("bronze_parsed_raw")
    .select(
        "path",
        concat_ws("\n\n", col("parsed.document.elements.content")).alias("full_text")
    )
    .writeStream.toTable("bronze_documents_text")
)

# Task 3: Extract fields
(
    spark.readStream.table("bronze_documents_text")
    .select(
        "path",
        ai_query("databricks-claude-sonnet-4", col("full_text")).alias("extracted"),
        lit("v1").alias("prompt_version")
    )
    .writeStream.toTable("bronze_documents_structured")
)</code></pre>



<p><strong>One clarification on duplicates:&nbsp;</strong>cloudFiles&nbsp;tracks files by path. If the same file is re-sent under the same name, the checkpoint skips it automatically. But a corrected document arriving under a new filename is a different path, so it is processed as a new row alongside the original. In practice, that means deduplication belongs in Silver, where&nbsp;ROW_NUMBER() OVER (PARTITION BY&nbsp;patient_ref,&nbsp;document_date,&nbsp;document_typeORDER BY _commit_timestamp&nbsp;DESC)&nbsp;can keep only the most recent extraction per logical document.&nbsp;</p>



<p>The three-task structure also helps with cost. Because parsing and extraction are separate tasks, you can update the prompt without reparsing the documents.&nbsp;ai_parse_document&nbsp;charges per page, so rerunning it on historical files every time you refine the extraction is&nbsp;wasted spend. If a new hospital uses “patiëntnummer” instead of “patient_ref”,&nbsp;or you decide to extract medications as well, you can reset only the Task 3 checkpoint and rerun&nbsp;ai_query&nbsp;against the already-parsed text in&nbsp;bronze_documents_text. The parse cost for existing files stays at zero.&nbsp;</p>



<p>Stamp each run with a new&nbsp;prompt_version, and both generations can coexist in the table. That keeps the audit trail intact and makes prompt changes much easier to manage.&nbsp;</p>



<p>Databricks publishes a&nbsp;<a href="https://github.com/databricks/bundle-examples/tree/main/contrib/job_with_ai_parse_document" target="_blank" rel="noreferrer noopener nofollow">Databricks Asset Bundle</a>&nbsp;that implements this same three-task streaming pattern as a deployable reference. In production, the stages are typically orchestrated in a&nbsp;<a href="https://docs.databricks.com/en/workflows/index.html" target="_blank" rel="noreferrer noopener nofollow">Databricks Workflow</a>: parse, assemble text, extract fields, then rebuild Silver and run a quality check. If Bronze fails, nothing downstream runs.&nbsp;</p>



<p>Here’s&nbsp;what a typical row looks like in&nbsp;bronze_documents_structured&nbsp;– the&nbsp;extracted&nbsp;column holds a parsed struct,&nbsp;queryable&nbsp;via dot notation in Silver:&nbsp;</p>



<p><strong><em>Table&nbsp;1:&nbsp;bronze_documents_structured&nbsp;Delta table.</em></strong>&nbsp;</p>



<pre class="wp-block-code"><code>
| File | Primary diagnosis | Follow-up required |
|---|---|---|
| `discharge_PT_2024_01812_digital.pdf` | Intertrochanteric femur fracture, right hip | true |
| `discharge_PT_2024_01401_digital.pdf` | Ischaemic stroke, left MCA territory | true |
| `discharge_PT_2024_00534_scanned.pdf` | Diabetic ketoacidosis (DKA), severe | true |
| `discharge_PT_2024_02534_scanned.pdf` | Hypertensive crisis with acute kidney injury | true |
| `discharge_PT_2024_02289_scanned.pdf` | Acute appendicitis, perforated | false |

</code></pre>



<h2 class="wp-block-heading">Silver: Mostly Just SQL</h2>



<p>Bronze handles&nbsp;the hard&nbsp;work. Silver is mostly SQL.&nbsp;</p>



<p>We have two tasks&nbsp;here, which are to: flatten the extracted&nbsp;struct&nbsp;into a&nbsp;queryable&nbsp;table and strip out the noise that arrived pre-baked in the text. As we mentioned above, because&nbsp;ai_parse_document&nbsp;processes the binary internally &#8211; hospital headers and footers, page stamps, etc.&nbsp;&#8211;&nbsp;end up in the parsed content alongside the actual clinical text. A SQL UDF in Silver keeps the cleanup reusable:&nbsp;</p>



<pre class="wp-block-code"><code>CREATE OR REPLACE FUNCTION strip_noise(text STRING)
RETURNS STRING
RETURN regexp_replace(
    regexp_replace(
        text,
        'Hospital Header Pattern',
        ''
    ),
    '\\n{3,}',
    '\\n\\n'
);</code></pre>



<p>Apply it to any free-text field where letterhead might bleed <em>in&nbsp;&#8211;&nbsp;strip_noise(extracted.primary_diagnosis</em>)&nbsp;in the SELECT below. Each hospital will have its own header pattern; build up a library as&nbsp;you&nbsp;onboard new institutions. The query itself is straightforward:&nbsp;</p>



<pre class="wp-block-code"><code>SELECT
    extracted.patient_ref,
    extracted.document_date,
    extracted.document_type,
    strip_noise(extracted.primary_diagnosis) AS primary_diagnosis,
    extracted.follow_up_required,
    datediff(current_date(), extracted.document_date) AS document_age_days,

    -- Placeholder: assumes a 30-day follow-up window.
    -- Extend the prompt to extract the actual follow-up date
    -- from the document if your use case requires it.
    date_add(extracted.document_date, 30) AS follow_up_due_date

FROM bronze_documents_structured
WHERE prompt_version = 'v1';
</code></pre>



<p><strong><em>Table&nbsp;2:&nbsp;silver_documents&nbsp;– flat,&nbsp;queryable, ready for Gold. Note that&nbsp;follow_up_due_date&nbsp;is a placeholder using a fixed 30-day window – real follow-up intervals vary by diagnosis and should ideally come from the document itself.</em></strong>&nbsp;</p>



<pre class="wp-block-code"><code>
| patient_ref | document_date | document_type | primary_diagnosis | follow_up_required | document_age_days | follow_up_due_date |
|------------|--------------|--------------|-------------------|-------------------|------------------|-------------------|
| be313e51... | 2024-11-20 | discharge_summary | Intertrochanteric femur fracture, right hip | true | 467 | 2024-12-20 |
| c9515e54... | 2024-07-08 | discharge_summary | Ischaemic stroke, left MCA territory | true | 602 | 2024-08-07 |
| 5cee6b7a... | 2024-09-01 | discharge_summary | Diabetic ketoacidosis (DKA), severe – Type 1 diabetes mellitus | true | 547 | 2024-10-01 |
| 9b150107... | 2024-09-15 | discharge_summary | Hypertensive crisis with acute kidney injury | true | 533 | 2024-10-15 |
| 1bf593cd... | 2024-05-22 | discharge_summary | Acute appendicitis, perforated | false | 649 | 2024-06-21 |

</code></pre>



<p>The same principle applies here as in Bronze: do the expensive work once and&nbsp;don’t&nbsp;repeat it.&nbsp;Materialise&nbsp;Silver as a table rather than a view&nbsp;&#8211;&nbsp;you pay a small compute cost once per run, but everything downstream reads from a clean, pre-computed dataset. Analyst queries are&nbsp;faster,&nbsp;Bronze&nbsp;isn’t&nbsp;being hit repeatedly, and the LLM only ever runs during ingestion. When you update to a new prompt version, new rows land under the new&nbsp;prompt_version&nbsp;and you update the filter.&nbsp;</p>



<p>At this point you have a clean, flat&nbsp;Silver&nbsp;layer&nbsp;&#8211;&nbsp;structured fields, noise stripped, prompt version tracked. What you build on top in Gold depends entirely on your use case, so we&nbsp;won’t&nbsp;dive into that here.&nbsp;</p>



<p>But before you go ahead and build any of this, there is a question worth asking first.&nbsp;</p>



<h2 class="wp-block-heading">Not Every Pipeline Needs an LLM&nbsp;</h2>



<p>In many healthcare pipelines, a large share of documents&nbsp;follow&nbsp;predictable templates. Discharge summaries from the same institution look&nbsp;roughly the&nbsp;same. Referral letters follow a standard structure. Where&nbsp;that’s&nbsp;true, an LLM is the wrong tool&nbsp;&#8211;&nbsp;you’re&nbsp;paying for flexibility you&nbsp;don’t&nbsp;need and accepting non-determinism you&nbsp;can’t&nbsp;afford.&nbsp;</p>



<p><a href="https://github.com/opendataloader-project/opendataloader-pdf" target="_blank" rel="noreferrer noopener nofollow">OpenDataLoader PDF</a>&nbsp;is one alternative worth knowing about.&nbsp;It’s&nbsp;open-source (Apache 2.0) and its local mode is fully deterministic, using the XY-Cut++ geometric algorithm for reading order and table detection&nbsp;&#8211;&nbsp;no model involved. Parsing cost drops to zero, and if you still need field&nbsp;extraction&nbsp;you can add&nbsp;ai_query&nbsp;on top. The limitation is template variance: if different hospitals label the same field differently (“Patiëntnummer” vs “Patient ID”),&nbsp;you’ll&nbsp;need an LLM to&nbsp;normalise&nbsp;them regardless. But where templates are consistent, deterministic parsing combined with regex is cheaper, faster, and fully reproducible.&nbsp;</p>



<p>Other tools sit at different points on this spectrum&nbsp;&#8211;&nbsp;layout-aware ML parsers like&nbsp;<a href="https://github.com/docling-project/docling" target="_blank" rel="noreferrer noopener nofollow">Docling</a>&nbsp;or&nbsp;<a href="https://github.com/opendatalab/MinerU" target="_blank" rel="noreferrer noopener nofollow">MinerU</a>&nbsp;in the middle, fully LLM-native approaches like&nbsp;<a href="https://docs.llamaindex.ai/en/stable/llama_cloud/llama_parse/" target="_blank" rel="noreferrer noopener nofollow">LlamaParse</a>&nbsp;at the other end. The trade-off holds across all of them: more convenience and flexibility, less determinism and cost control.&nbsp;</p>



<h2 class="wp-block-heading">Which Approach Is Right?&nbsp;</h2>



<p>The honest summary before you decide:&nbsp;</p>



<pre class="wp-block-code"><code>
Scenario                                               Priority
----------------------------------------------------------------------------
Consistent templates, digitally-born PDFs              Reproducibility and auditability
                                                       are non-negotiable

Layout or labels vary across institutions              Cost control and reproducible
                                                       extraction

Mixed formats, variable document structure             Fast time-to-value over strict
                                                       operational predictability

</code></pre>



<h2 class="wp-block-heading">When&nbsp;ai_parse_document&nbsp;<em>Is</em>&nbsp;a Good Fit&nbsp;</h2>



<p>It’s&nbsp;worth being explicit:&nbsp;ai_parse_document&nbsp;is not a bad tool. It&nbsp;favours&nbsp;fast setup, broad document support, and flexible extraction over strict determinism and cost efficiency at scale.&nbsp;</p>



<p>That makes it a good fit for:&nbsp;</p>



<ul class="wp-block-list">
<li><strong>Proofs of concept.</strong>&nbsp;When you need to&nbsp;validate&nbsp;whether useful data can be extracted from a document set quickly – without building a custom parsing stack.&nbsp;</li>
</ul>



<ul class="wp-block-list">
<li><strong>Low-to-medium volume workflows.</strong>&nbsp;Where reprocessing cost is manageable and&nbsp;perfect&nbsp;reproducibility is not&nbsp;required.&nbsp;</li>
</ul>



<ul class="wp-block-list">
<li><strong>Human-in-the-loop processes.</strong>&nbsp;For example, pre-processing invoices, claims, or intake forms before review – where the output is verified downstream rather than trusted directly.&nbsp;</li>
</ul>



<ul class="wp-block-list">
<li><strong>RAG ingestion pipelines.</strong>&nbsp;Where layout-aware parsing (sections, tables, figures, bounding boxes) produces better chunks than plain text, and exact field-level consistency matters less than good retrieval.&nbsp;</li>
</ul>



<ul class="wp-block-list">
<li><strong>Mixed-format document collections.</strong>&nbsp;When dealing with PDFs, scans, DOCX, and images in a single pipeline without stitching together multiple parsers.&nbsp;</li>
</ul>



<p>In those scenarios,&nbsp;the convenience&nbsp;and flexibility can outweigh&nbsp;the trade-offs.&nbsp;</p>



<p>The Databricks-native route is the most convenient starting point – and in some cases, the right end state – but not always where you land after a full production evaluation. If you&nbsp;can’t&nbsp;tolerate output drift,&nbsp;don’tstart with an LLM. Most teams&nbsp;don’t&nbsp;need smarter parsing&nbsp;&#8211;&nbsp;they need more predictable systems.&nbsp;</p>



<p>I had&nbsp;a great time&nbsp;working on this and if&nbsp;you’re&nbsp;building something similar or want to discuss some points I missed – you can find me on&nbsp;<a href="https://www.linkedin.com/in/andy-h0925/" target="_blank" rel="noreferrer noopener nofollow">LinkedIn</a>.&nbsp;</p></article>
]]></content:encoded>
    </item>
  </channel>
</rss>
